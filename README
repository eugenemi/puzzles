coding puzzles, interview questions, longer problems

github instructions:
Global setup:
Download and install Git
git config --global user.name "Your Name"
git config --global user.email eugenemi@gmail.com

Next steps:
mkdir puzzles
cd puzzles
git init
touch README
git add README
git commit -m 'first commit'
git remote add origin git@github.com:eugenemi/puzzles.git
git push origin master

Existing Git Repo?
cd existing_git_repo
git remote add origin git@github.com:eugenemi/puzzles.git
git push origin master
